#include <bits/stdc++.h>

using namespace std;

int main()
{
    int n, m;

    cin >> n >> m;

    vector<vector<int>>Graph(n+1);

    for( int i = 1, k = 2; i <= n; i++, k++ )
    {
        for( int j = 1; j <= i; j++ )
        {
            int vertex1, vertex2;

            vertex1 = j;

            vertex2 = k;
        }
    }
}

